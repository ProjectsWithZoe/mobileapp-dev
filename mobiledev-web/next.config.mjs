/** @type {import('next').NextConfig} */
const nextConfig = {
  // Security headers are handled in vercel.json for edge delivery.
  // Images from src/assets are imported directly as modules — no external domains needed.
}

export default nextConfig
